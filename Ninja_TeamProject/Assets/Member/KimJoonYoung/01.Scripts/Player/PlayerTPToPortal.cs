using Member.KimJoonYoung._01.Scripts.UI.Portal;
using UnityEngine;

public class PlayerTPToPortal : MonoBehaviour
{
    [SerializeField] private Transform portalTransform;
    
    private void Start()
    {
        // PortalButtonAction.Instance.OnButtonPress += 
    }
}
